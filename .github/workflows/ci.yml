name: PR tests validation

on:
  pull_request:
    branches:
      - main

jobs:
  execute_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: yarn
      - name: Test the app
        run: yarn test
